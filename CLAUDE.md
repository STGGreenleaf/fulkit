# Fulkit Monorepo

Two projects live here:

- **fulkit_site/** — the Fulkit product (Next.js). Read `fulkit_site/CLAUDE.md` for dev context.
- **ChappieBrain/** — Collin's second brain (Obsidian vault). Not deployed. Not in git.

When starting a session, ask which project, then read that project's CLAUDE.md.
